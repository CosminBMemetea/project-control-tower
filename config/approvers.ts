// The 3 senior managers required to sign off on the 120% Business
// Innovation Process approval, per project. Do not hardcode manager
// names anywhere else — every project gets one ManagerApproval row
// per entry here (see `prisma/seed.ts`).
export const APPROVERS: string[] = ["Manager One", "Manager Two", "Manager Three"];
